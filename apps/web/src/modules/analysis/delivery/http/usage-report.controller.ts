import type { GetUsageReportService } from '../../application/services/get-usage-report.service';
export class UsageReportController { constructor(private readonly service: GetUsageReportService) {} async handle(request: Request) { const url = new URL(request.url); const pageSize = Number(url.searchParams.get('pageSize') ?? 100); return Response.json(await this.service.execute({ cursor: url.searchParams.get('cursor') ?? undefined, pageSize })); } }
