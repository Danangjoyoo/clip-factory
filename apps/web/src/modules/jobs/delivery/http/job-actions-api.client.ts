export async function postJobAction(projectId: string, action: string, body: unknown = {}) { const response = await fetch(`/api/projects/${projectId}/jobs/actions`, { method: 'POST', headers: { 'content-type': 'application/json', 'idempotency-key': crypto.randomUUID() }, body: JSON.stringify({ action, ...body }) }); if (!response.ok) throw new Error(`Job action failed: ${response.status}`); return response.json(); }
