import type { ProcessingPresentation } from './processing.presentation';
import { StageTimeline } from './StageTimeline'; import { BudgetActions } from './BudgetActions'; import { UncertainPaidCallPanel } from './UncertainPaidCallPanel'; import { SanitizedLogList } from './SanitizedLogList';
export function ProcessingView({ value, actions = {} }: { value: ProcessingPresentation; actions?: { cancel?: () => void; retry?: (v: { acknowledgePossiblePriorSpend: true }) => void; abandon?: () => void } }) { const running = value.state === 'RUNNING' && typeof value.percent === 'number'; return <main><h1>Processing</h1><StageTimeline stages={value.stages} />{running && <div role="progressbar" aria-label={`${value.stage ?? 'Processing'} progress`} aria-valuenow={value.percent} aria-valuemin={0} aria-valuemax={100} />}{value.eta && value.state === 'RUNNING' && <p>{value.eta}</p>}{!value.workerOnline && <p role="status">Worker offline — job remains queued</p>}<SanitizedLogList logs={value.logs} />{value.state === 'AWAITING_BUDGET' && <BudgetActions onCancel={actions.cancel} />}{value.state === 'PAID_CALL_UNCERTAIN' && <UncertainPaidCallPanel possibleSpend={value.possibleSpend ?? 'Possible spend unknown'} onAuthorize={actions.retry ?? (() => undefined)} onAbandon={actions.abandon ?? (() => undefined)} />}<button onClick={actions.cancel}>Cancel</button></main>; }
