import { models, type CatalogView, type ModelId } from './new-project.presentation';
export function AnalysisSettings({ discover, model, reasoning, catalog, onDiscover, onModel, onReasoning }: { discover: boolean; model: ModelId; reasoning: string; catalog: CatalogView; onDiscover: (value: boolean) => void; onModel: (value: ModelId) => void; onReasoning: (value: string) => void }) {
  return <section aria-label="Analysis settings"><label><input type="checkbox" checked={discover} onChange={(e) => onDiscover(e.target.checked)} /> Discover highlights with OpenAI</label>{discover ? <><label>Model<select aria-label="Model" value={model} onChange={(e) => onModel(e.target.value as ModelId)}>{(Object.keys(models) as ModelId[]).map((id) => <option key={id} value={id} disabled={catalog.models?.[id]?.available === false}>{id}{catalog.models?.[id]?.available === false ? ' (unavailable)' : ''}</option>)}</select></label><label>Reasoning<select aria-label="Reasoning" value={reasoning} onChange={(e) => onReasoning(e.target.value)}>{models[model].map((effort) => <option key={effort}>{effort}</option>)}</select></label><small>Generated-token ceiling includes reasoning and visible output.</small></> : <p>No cloud AI / no API cost</p>}</section>;
}
