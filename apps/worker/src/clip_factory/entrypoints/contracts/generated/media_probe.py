# generated by datamodel-codegen:
#   filename:  media-probe.schema.json

from __future__ import annotations

from enum import Enum
from typing import Dict, Literal

from pydantic import BaseModel, ConfigDict, conint, constr


class Container(Enum):
    mp4 = "mp4"
    mov = "mov"
    matroska = "matroska"
    webm = "webm"


class Video(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    codec: constr(min_length=1)
    width: conint(ge=1)
    height: conint(ge=1)
    frameRateNumerator: conint(ge=1)
    frameRateDenominator: conint(ge=1)


class Audio(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    codec: constr(min_length=1)
    sampleRateHz: conint(ge=1)
    channels: conint(ge=1)


class MediaProbe(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    schemaVersion: Literal["1.0.0"]
    durationMs: conint(ge=1)
    sizeBytes: conint(ge=1)
    container: Container
    video: Video
    audio: Audio
    formatTags: Dict[str, str]
