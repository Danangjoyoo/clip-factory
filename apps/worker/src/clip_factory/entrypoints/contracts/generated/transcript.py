# generated by datamodel-codegen:
#   filename:  transcript.schema.json

from __future__ import annotations

from enum import Enum
from typing import List, Literal, Optional
from uuid import UUID

from pydantic import BaseModel, ConfigDict, conint, constr


class Segment(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    text: constr(min_length=1)
    startMs: conint(ge=0)
    endMs: conint(ge=0)
    wordStartIndex: conint(ge=0)
    wordEndIndex: conint(ge=0)


class Word(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    text: constr(min_length=1)
    startMs: conint(ge=0)
    endMs: conint(ge=0)
    confidenceMicros: Optional[conint(ge=0, le=1000000)] = None


class Backend(Enum):
    MLX_WHISPER = 'MLX_WHISPER'
    FAKE = 'FAKE'


class Transcript(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schemaVersion: Literal['1.0.0']
    transcriptId: UUID
    languageTag: constr(min_length=1)
    text: str
    segments: List[Segment]
    words: List[Word]
    backend: Backend
    model: constr(min_length=1)
    modelRevision: constr(min_length=1)
    weightsSha256: Optional[constr(pattern=r'^[a-f0-9]{64}$')] = None
    durationMs: conint(ge=0)
