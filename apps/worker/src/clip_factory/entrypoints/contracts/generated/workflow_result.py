# generated by datamodel-codegen:
#   filename:  workflow-result.schema.json

from __future__ import annotations

from enum import Enum
from typing import Any, Dict, List, Literal, Optional
from uuid import UUID

from pydantic import AwareDatetime, BaseModel, ConfigDict, constr


class Status(Enum):
    COMPLETED = "COMPLETED"
    FAILED = "FAILED"
    CANCELLED = "CANCELLED"


class TranscriptObject(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    bucket: Literal["clip-factory"]
    key: constr(min_length=1)
    versionId: Optional[constr(min_length=1)] = None
    sha256: constr(pattern=r"^[a-f0-9]{64}$")


class Category(Enum):
    RETRYABLE = "RETRYABLE"
    NON_RETRYABLE = "NON_RETRYABLE"
    WAITING = "WAITING"
    CANCELLED = "CANCELLED"


class Error(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    code: constr(min_length=1)
    category: Category
    retryable: bool
    message: constr(min_length=1)
    requiredAction: Optional[constr(min_length=1)] = None
    details: Dict[str, Any]


class WorkflowResult(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    schemaVersion: Literal["1.0.0"]
    workflowId: UUID
    projectId: UUID
    status: Status
    transcriptObject: Optional[TranscriptObject] = None
    clipIds: List[UUID]
    error: Optional[Error] = None
    completedAt: AwareDatetime
