{
  "name": "@clip-factory/contracts",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "check:compatibility": "node scripts/check-compatibility.mjs",
    "lint": "eslint scripts && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run && node scripts/check-compatibility.mjs && node ../config/scripts/generate-cost-conformance.mjs --check"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  },
  "devDependencies": {
    "json-schema-to-typescript": "15.0.4"
  }
}
