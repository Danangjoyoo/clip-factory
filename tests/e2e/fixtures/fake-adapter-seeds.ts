/** Deterministic names shared by browser journeys and future fake adapters. */
export const fakeAdapterSeeds = {
  source: 'talking-head.mp4',
  transcript: 'Hello from the synthetic transcript.',
} as const;
